{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "js/index.js",
  "scripts": {
    "clean": "rm -rf .app",
    "start": "node --experimental-modules .app/index.mjs",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.23",
    "typescript": "^4.9.1-beta"
  }
}
