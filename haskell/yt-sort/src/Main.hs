-- Get the information for the last 10 youtube history items - have they been fully viewed?

-- Get the information for the last video of a channel.

-- Get all subscriptions for a user (the logged in user, mayhaps)

module Main (main) where

--  import Network.Google
--  import Network.Google.YouTube
--  import Network.Google.Resource.YouTube.Subscriptions.List
--
--  mySubs :: IO a
--  mySubs = do
--      lgr <- newLogger Debug stdout
--      _env <- newEnv <&> (envLogger .~ lgr) . (envScopes .~ youTubeReadOnlyScope)
--      pure ()
--

main ∷ IO ()
main = pure ()
